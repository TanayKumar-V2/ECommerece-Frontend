import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Women's Collection | Viraasat",
  description:
    "Discover Viraasat's women's collection — elegant fashion inspired by Indian heritage, crafted for the modern woman.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
